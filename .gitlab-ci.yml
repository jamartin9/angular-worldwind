image: node:latest

pages:
  stage: deploy
  script:
  - npm install
  - npm run ng build --aot=true --environment=prod --sourcemap=false --base-href="https://spaz.gitlab.io/angular-cesium/"
  artifacts:
    paths:
    - public
  only:
  - master